// import { normalize, schema, arrayOf } from 'normalizr'
//
// export default (originalData) => {
//
//
//   // const subtopic = new schema.Entity('subtopics', {  } , { idAttribute: '_id'})
//   // const topic = new schema.Entity('topics', { subtopics: [ subtopic ]}, { idAttribute: '_id' })
//
//   const normalizedData = normalize(originalData.domain.topics, topicSchema)
// }
